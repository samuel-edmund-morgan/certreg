<?php
// Example application configuration. Copy to config.php and fill values.
// REMOVE comments with secrets in real config.
return [
    // === Database (primary app user) ===
    'db_host' => '127.0.0.1',
    'db_name' => 'enter-db-name',
    'db_user' => 'enter-db-username',
    'db_pass' => 'enter-db-password',

    // Optional: dedicated read-only user for public status/verification endpoints (least privilege)
    // Leave blank to reuse primary user (less secure):
    'db_public_user' => '',
    'db_public_pass' => '',

    // === Branding defaults (can be overridden by DB branding_settings and per-organization rows) ===
    // Multiline site name: use literal \n to force line break in header (replaced with <br>)
    'site_name' => "Реєстр нагород\n Національної академії Служби безпеки України",
    'logo_path' => '/assets/logo.png',
    'favicon_path' => '/assets/favicon.ico',
    // Base certificate background (operators may later pick org-specific / template-specific once implemented)
    'cert_template_path' => '/files/cert_template.jpg',

    // === Canvas coordinates (client-side rendering only, LEGACY) ===
    // NOTE: These global coords are a legacy fallback used by the issuance preview.
    // Phase 1 templates feature stores per-template metadata (future step) that will supersede this array.
    // Keep values here for backward compatibility until template_update + template coords UI lands.
    'coords' => [
        'name'   => ['x'=>600, 'y'=>420, 'size'=>28, 'angle'=>0],
        'id'     => ['x'=>600, 'y'=>445, 'size'=>20, 'angle'=>0],
        'extra'  => ['x'=>600, 'y'=>520, 'size'=>24, 'angle'=>0],
        'date'   => ['x'=>600, 'y'=>570, 'size'=>24, 'angle'=>0],
        'expires'=> ['x'=>600, 'y'=>600, 'size'=>20, 'angle'=>0],
        'qr'     => ['x'=>150, 'y'=>420, 'size'=>220],
        'int'    => ['x'=>820, 'y'=>670, 'size'=>14, 'angle'=>0],
    ],

    // Stable short organization code used in canonical token string (immutable)
    'org_code' => 'ORG-CERT',
    // Sentinel date representing “безтерміновий” (infinite validity)
    'infinite_sentinel' => '4000-01-01',
    // Public verification page URL (canonical base). Supply full absolute URL including https://
    'canonical_verify_url' => 'https://example.org/verify.php',

    // === Rate limiting: [maxActions, windowSeconds] ===
    'rate_limits' => [
        'login'     => [8,   300],   // 8 attempts per 5 min
        'status'    => [30,   60],   // public status lookups
        'register'  => [60, 1800],   // token issuance
        'revoke'    => [20,  600],
        'unrevoke'  => [20,  600],
        'delete'    => [10,  600],
    ],

    // === Templates / media settings ===
    // Max width for generated preview.jpg (pixels). Height auto-calculated to preserve aspect.
    'template_preview_max_width' => 800,
    // When true (or if CERTREG_DEBUG env var set), template_create will include extra diagnostics on file_store errors.
    'debug_templates' => false,
];